import { AllProjectIssues } from './all-project-issues';

describe('AllProjectIssues', () => {
  it('should create an instance', () => {
    expect(new AllProjectIssues()).toBeTruthy();
  });
});
