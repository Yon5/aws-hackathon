import { AllProjectTraceabilityTotal } from './all-project-traceability-total';

describe('AllProjectTraceabilityTotal', () => {
  it('should create an instance', () => {
    expect(new AllProjectTraceabilityTotal()).toBeTruthy();
  });
});
