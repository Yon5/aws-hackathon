import { AllProjectTraceability } from './all-project-traceability';

describe('AllProjectTraceability', () => {
  it('should create an instance', () => {
    expect(new AllProjectTraceability()).toBeTruthy();
  });
});
